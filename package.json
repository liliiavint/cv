{
  "name": "cv",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "dead-server --host=localhost --port=4880",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lilia (https://github.com/liliiavint)",
  "license": "ISC",
  "dependencies": {
    "node-modules": "^1.0.1"
  },
  "devDependencies": {
    "dead-server": "^1.0.9"
  }
}
